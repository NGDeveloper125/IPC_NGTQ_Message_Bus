[package]
name = "bus_service_integration_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
bus_service_client = { path = "../bus_service_client"}
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
serial_test = "2.0.0"